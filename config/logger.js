import config from './envConfig';

export default class Logger {

  static info(text) {
    console.log(text);
  }

  static log(text) {
    console.log(text);
  }

  static error(text) {
    console.log(text);
  }
}
